# Third-party SDKs, fetched separately (see README)
hudreload/sdk/
app/webview2/

# Build output (app/CSSHudEditor.exe is kept in the repo as the download)
hudreload/build/
app/build/
*.tmp

# Working notes
PLAN.md
